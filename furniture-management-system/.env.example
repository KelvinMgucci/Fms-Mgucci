# Server-only — used by next.config.mjs to proxy /api/* to the Django backend.
# Local dev default matches `python manage.py runserver` on port 8000.
# In Vercel, set this to your Render backend URL, e.g. https://fms-backend.onrender.com
BACKEND_URL=http://localhost    